const baseUrl = window.location.origin;

const urls = {
	githubRepo: 'https://github.com/mugnainiguillermo/spa-odinbook',
	baseUrl: baseUrl,
	homePage: baseUrl,
	newsPage: `${baseUrl}/news`,
	profilePage: `${baseUrl}/profile`,
};

export default urls;