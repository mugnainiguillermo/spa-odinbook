import { createStyles } from '@material-ui/core';

const styles = (theme) => {
  return createStyles({
    cardMedia: {
      height: '400px',
    },
    likeCounter: {
      marginLeft: '15px',
    },
  });
};

export default styles;