import { createStyles } from '@material-ui/core';

const styles = (theme) => {
  return createStyles({
    root: {
      padding: '10px',
    },
    avatar: {
      width: '60px',
      height: '60px',
      marginRight: '20px',
    },
  });
};

export default styles;